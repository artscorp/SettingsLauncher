# 🚀 Инструкция по сборке APK файла

## 📱 Настройки Launcher - Android приложение

### 🎯 Описание
Простое Android приложение для быстрого запуска системных настроек и других приложений через кнопки в удобном интерфейсе.

### 📂 Структура проекта
```
SettingsLauncher/
├── app/
│   ├── src/main/
│   │   ├── java/com/minimax/settingslauncher/
│   │   │   └── MainActivity.java
│   │   ├── res/
│   │   │   ├── layout/activity_main.xml
│   │   │   ├── values/colors.xml
│   │   │   ├── values/strings.xml
│   │   │   └── drawable/button_background.xml
│   │   └── AndroidManifest.xml
│   └── build.gradle
├── build.gradle
├── gradle.properties
└── settings.gradle
```

### 🔧 Методы сборки APK

#### 📱 Метод 1: Android Studio (РЕКОМЕНДУЕТСЯ)

1. **Установите Android Studio:**
   - Скачайте с [developer.android.com](https://developer.android.com/studio)
   - Установите Android SDK (API 21+)

2. **Импортируйте проект:**
   - Откройте Android Studio
   - Выберите "Open an existing Android Studio project"
   - Выберите папку `SettingsLauncher`

3. **Синхронизация:**
   - Android Studio автоматически предложит синхронизировать проект
   - Дождитесь завершения загрузки зависимостей

4. **Сборка APK:**
   - В меню выберите: `Build` → `Build Bundle(s) / APK(s)` → `Build APK(s)`
   - После сборки нажмите "locate" чтобы найти APK файл
   - APK будет создан в `app/build/outputs/apk/debug/app-debug.apk`

#### 💻 Метод 2: Командная строка (Терминал)

1. **Установите Android SDK:**
   ```bash
   # Ubuntu/Debian
   sudo apt install android-sdk
   
   # macOS
   brew install android-sdk
   ```

2. **Настройте переменные окружения:**
   ```bash
   export ANDROID_HOME=/path/to/android-sdk
   export PATH=$PATH:$ANDROID_HOME/tools:$ANDROID_HOME/platform-tools
   ```

3. **Соберите APK:**
   ```bash
   cd SettingsLauncher
   ./gradlew assembleDebug
   ```

4. **Найдите APK:**
   - APK будет в: `app/build/outputs/apk/debug/app-debug.apk`

#### 📱 Метод 3: Онлайн сборка (APK Builder)

1. **Используйте онлайн сервисы:**
   - [ApkOnline](https://www.apkonline.net/)
   - [MIT App Inventor](http://appinventor.mit.edu/)

2. **Загрузите исходники:**
   - Архивируйте папку `app/src/main`
   - Загрузите на онлайн сервис

### ⚡ Быстрая сборка (если у вас есть Android Studio)

```bash
# Клонируйте проект
git clone [url-проекта]
cd SettingsLauncher

# Откройте в Android Studio
# Нажмите Build → Build APK
```

### 📋 Требования для сборки

- **Android Studio** версии 4.0+
- **Java Development Kit (JDK)** 8 или выше
- **Android SDK** API уровень 21+
- **Gradle** версии 7.0+

### 🔒 Подпись APK (для публикации)

1. **Создайте keystore:**
   ```bash
   keytool -genkey -v -keystore settings-launcher.keystore \
           -alias settings-launcher -keyalg RSA -keysize 2048 -validity 10000
   ```

2. **Подпишите APK:**
   ```bash
   jarsigner -verbose -sigalg SHA1withRSA -digestalg SHA1 \
             -keystore settings-launcher.keystore app-release-unsigned.apk settings-launcher
   ```

3. **Выровняйте APK:**
   ```bash
   zipalign -v 4 app-release-unsigned.apk SettingsLauncher-v1.0.apk
   ```

### 📱 Установка APK на устройство

1. **Включите разработчика:**
   - Настройки → О телефоне → Нажмите 7 раз на "Номер сборки"

2. **Включите установку из неизвестных источников:**
   - Настройки → Безопасность → Неизвестные источники

3. **Установите APK:**
   ```bash
   # Через ADB
   adb install app-debug.apk
   
   # Или просто скопируйте файл на устройство и установите
   ```

### 🎮 Функции приложения

Приложение имеет кнопки для запуска:
- ⚙️ Системные настройки
- 📷 Камера
- 🌐 Браузер
- 🧮 Калькулятор

### 🛠️ Настройка приложения

Чтобы добавить новые кнопки, отредактируйте:
- `MainActivity.java` - логику запуска
- `activity_main.xml` - интерфейс пользователя
- `strings.xml` - тексты кнопок

### ❓ Возможные проблемы

1. **Ошибка "SDK not found":**
   - Установите Android SDK через Android Studio
   - Настройте ANDROID_HOME

2. **Ошибка "Build failed":**
   - Обновите Gradle в Android Studio
   - Проверьте версии зависимостей

3. **Приложение не устанавливается:**
   - Включите "Неизвестные источники"
   - Проверьте версию Android (минимум API 21)

### 📞 Поддержка

Если у вас возникли проблемы:
1. Проверьте версию Android Studio
2. Обновите Android SDK
3. Очистите проект: Build → Clean Project
4. Пересоберите: Build → Rebuild Project

---
**Автор:** MiniMax Agent  
**Версия проекта:** 1.0  
**Дата:** 2025-11-24